// import bcrypt from 'bcrypt';

export async function editProfileRoute(fastify)
{
	fastify.post('/changeInfo', async (request, reply) => {
		// const { currentPassword, changePassword, repeatPassword, changeMail } = request.body;
		
		
	});
}